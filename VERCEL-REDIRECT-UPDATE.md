# Vercel 域名重定向配置更新指南

## 当前状态
- ✅ 主域名: `www.mhxy-helper.com`
- ✅ 次域名: `mhxy-helper.com` (307 临时重定向)
- ⚠️ 需要改进: 307 → 301 (永久重定向)

## 为什么要改为 301？

| 特性 | 307 临时重定向 | 301 永久重定向 |
|------|---------------|---------------|
| SEO 效果 | ⚠️ 较差 | ✅ 最佳 |
| PageRank 传递 | ❌ 不完全传递 | ✅ 完全传递 |
| 搜索引擎收录速度 | 🐌 较慢 | 🚀 快速 |
| 浏览器缓存 | ❌ 不缓存 | ✅ 可缓存 |
| 适用场景 | 临时维护 | 永久域名迁移 |

**结论**: 对于我们这种"www 作为主域名"的永久配置，**必须使用 301**！

---

## 📋 更新步骤

### 方法 1: Vercel Dashboard 手动配置

1. **登录 Vercel Dashboard**
   - 访问: https://vercel.com/dashboard
   - 选择项目: `mhxy_tool_web`

2. **进入 Domains 设置**
   - 点击 Settings → Domains
   - 找到 `mhxy-helper.com` 域名

3. **修改重定向设置**
   - 点击 `mhxy-helper.com` 旁边的 **⋮** (三个点)
   - 选择 "Edit"
   - 找到 "Redirect to" 选项
   - 确认重定向到 `www.mhxy-helper.com`
   - **重要**: 检查是否有 "Permanent Redirect (301)" 选项
     - ✅ 如果有，勾选它
     - ❌ 如果没有，说明 Vercel 默认使用 308（也是永久重定向，效果类似）

4. **保存并验证**
   - 点击 Save
   - 等待 DNS 生效（通常 1-5 分钟）

### 方法 2: 使用 vercel.json 配置文件（推荐）

在项目根目录创建或更新 `vercel.json`:

```json
{
  "redirects": [
    {
      "source": "https://mhxy-helper.com/:path*",
      "destination": "https://www.mhxy-helper.com/:path*",
      "permanent": true,
      "statusCode": 301
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        },
        {
          "key": "Referrer-Policy",
          "value": "strict-origin-when-cross-origin"
        }
      ]
    }
  ]
}
```

**配置说明**:
- `source`: 匹配所有来自 `mhxy-helper.com` 的请求（包括子路径）
- `destination`: 重定向到 `www.mhxy-helper.com` 对应路径
- `permanent: true` + `statusCode: 301`: 确保使用 301 永久重定向
- `headers`: 额外添加了安全相关的 HTTP 头

**部署步骤**:
```bash
# 1. 创建或更新 vercel.json
cat > vercel.json << 'EOF'
{配置内容见上方}
EOF

# 2. 提交到 Git
git add vercel.json
git commit -m "Add 301 permanent redirect in vercel.json"
git push

# 3. Vercel 会自动重新部署
```

---

## ✅ 验证重定向是否生效

### 方法 1: 使用 curl 命令

```bash
curl -I https://mhxy-helper.com
```

**期望输出**:
```
HTTP/2 301
location: https://www.mhxy-helper.com/
...
```

**关键点**:
- ✅ 状态码是 `301` 或 `308` (永久重定向)
- ❌ 如果是 `307` 或 `302` (临时重定向)，需要继续调整

### 方法 2: 使用在线工具

访问: https://httpstatus.io/
- 输入: `https://mhxy-helper.com`
- 检查响应状态码
- 确认重定向链: `mhxy-helper.com` → `www.mhxy-helper.com` (301)

### 方法 3: 浏览器开发者工具

1. 打开 Chrome DevTools (F12)
2. 切换到 Network 标签
3. 访问 `https://mhxy-helper.com`
4. 查看第一个请求的状态码
5. ✅ 应该显示 `301 Moved Permanently`

---

## 🎯 完成后的检查清单

- [ ] 重定向状态码改为 301 (或 308)
- [ ] 测试主域名访问: `https://www.mhxy-helper.com` ✅ 正常
- [ ] 测试重定向: `https://mhxy-helper.com` → `https://www.mhxy-helper.com` (301)
- [ ] 测试带路径的重定向: `https://mhxy-helper.com/calculator` → `https://www.mhxy-helper.com/calculator`
- [ ] 检查 sitemap.xml: 访问 `https://www.mhxy-helper.com/sitemap.xml`，所有 URL 都是 www 版本
- [ ] 检查 robots.txt: 访问 `https://www.mhxy-helper.com/robots.txt`

---

## 📊 SEO 提交指南

### Google Search Console

1. **添加资源**
   - 访问: https://search.google.com/search-console
   - 点击 "添加资源"
   - 选择 "网址前缀"
   - 输入: `https://www.mhxy-helper.com`
   - 验证所有权 (推荐使用 HTML 标签验证)

2. **验证代码添加到 layout.tsx**
   ```typescript
   verification: {
     google: 'your-google-verification-code',
   }
   ```

3. **提交 Sitemap**
   - 在 Google Search Console 左侧菜单选择 "站点地图"
   - 输入: `https://www.mhxy-helper.com/sitemap.xml`
   - 点击 "提交"

4. **设置域名属性（可选）**
   - 添加第二个资源: `https://mhxy-helper.com`
   - 在设置中将 www 版本设为"首选域名"

### 百度站长平台

1. **添加网站**
   - 访问: https://ziyuan.baidu.com
   - 点击 "用户中心" → "站点管理" → "添加网站"
   - 输入: `https://www.mhxy-helper.com`
   - 选择站点属性: 游戏 / 工具

2. **验证网站**
   - 选择 "HTML 标签验证"
   - 添加验证代码到 layout.tsx:
     ```typescript
     verification: {
       baidu: 'your-baidu-verification-code',
     }
     ```
   - 点击 "完成验证"

3. **提交 Sitemap**
   - 左侧菜单: "数据引入" → "链接提交"
   - 选择 "sitemap"
   - 输入: `https://www.mhxy-helper.com/sitemap.xml`
   - 点击 "提交"

4. **主动推送（推荐）**
   - 百度提供的主动推送 API 可以加快收录
   - 获取推送接口: "数据引入" → "链接提交" → "主动推送"
   - 将推送接口添加到部署脚本

---

## 🚀 后续优化建议

1. **监控 SEO 表现**
   - 每周检查 Google Search Console 的收录情况
   - 查看"网页体验"报告
   - 关注"核心网页指标"

2. **外链建设**
   - 在梦幻西游相关论坛发布工具介绍（使用 www 域名）
   - 百度贴吧、NGA、17173 等平台推广
   - 确保所有外链都使用 `https://www.mhxy-helper.com`

3. **内容优化**
   - 定期更新攻略内容
   - 根据 Google Search Console 的"查询"数据优化关键词
   - 增加长尾关键词文章

4. **性能优化**
   - 使用 PageSpeed Insights 测试: https://pagespeed.web.dev/
   - 目标: 移动端和桌面端都达到 90+ 分
   - 优化图片、字体、JavaScript 加载

---

## 📝 常见问题

**Q: 为什么 Vercel 显示的是 307 而不是 301？**
A: Vercel 的 Dashboard 可能默认使用 307。建议使用 `vercel.json` 明确指定 301。

**Q: 改为 301 后，需要多久搜索引擎才会更新？**
A:
- Google: 通常 1-2 周
- 百度: 可能需要 2-4 周
- 建议同时提交到 Search Console 加快收录

**Q: 如果同时存在 www 和非 www 的收录怎么办？**
A:
1. 在 Google Search Console 设置"首选域名"为 www
2. 所有新内容都使用 www URL
3. 老的非 www URL 会逐步被 301 重定向更新

**Q: 需要更新旧的外链吗？**
A:
- 如果可以修改，尽量更新为 www 版本
- 如果无法修改，301 重定向会自动处理，但会损失一点 SEO 权重

---

**最后更新**: 2025-01-05
**维护者**: Claude & 用户

# 梦幻西游炼妖助手 - 完整调研文档

> 项目目标：为梦幻西游畅玩服玩家提供精确的炼妖概率计算工具，通过Google AdSense实现流量变现

---

## 目录

1. [项目背景](#项目背景)
2. [梦幻西游炼妖系统深度解析](#梦幻西游炼妖系统深度解析)
3. [畅玩服市场分析](#畅玩服市场分析)
4. [产品设计方案](#产品设计方案)
5. [技术架构设计](#技术架构设计)
6. [核心算法实现](#核心算法实现)
7. [数据库设计](#数据库设计)
8. [SEO与变现策略](#seo与变现策略)
9. [侵权规避策略](#侵权规避策略)
10. [开发路线图](#开发路线图)

---

## 项目背景

### 市场机会

- **畅玩服火爆**: 2025年7月，梦幻西游电脑版同时在线人数突破**275万**，创历史新高
- **新区频繁开放**: 2025年7月单次开放10个畅玩服新区，市场需求旺盛
- **玩家特征**: 25-35岁成年玩家为主，付费意愿强，喜欢理性分析
- **工具需求**: 现有工具(17173、官方工具箱)功能简单，缺少精确概率计算

### 竞争优势

| 维度 | 现有工具 | 我们的产品 |
|------|----------|------------|
| 概率计算 | 仅显示范围 | 精确到小数点，基于数学模型 |
| 必带技能 | 未考虑 | 完整支持必带技能规则 |
| 成本分析 | 无 | 期望收益、回本概率、性价比分析 |
| 用户体验 | 界面老旧 | 现代化UI，移动端友好 |
| 模拟功能 | 无 | 支持模拟100次炼妖的结果分布 |

---

## 梦幻西游炼妖系统深度解析

### 1. 炼妖基础机制

#### 1.1 炼妖流程

```
玩家携带主宠 + 副宠 → 长安马婆婆 → 消耗金币 → 生成结果宠物
```

**等级要求**:
- 两只宠物等级必须 ≥30级
- 两只宠物等级之和 ≥60级
- 结果宠物等级 = (主宠等级 + 副宠等级) / 2

**成本构成**:
- 基础金币消耗: (主宠等级 + 副宠等级) × 500 游戏币/次
- 胚子成本: 5万-200万不等(取决于资质和技能)
- 特殊道具: 炼妖石等(可选)

#### 1.2 结果宠物种类概率

根据用户提供的规则和网络调研验证：

```javascript
// 基础概率模型
const resultProbability = {
  wild: 0.10,              // 野生宠物 10% (可修改)
  special: 0.10,           // 大海龟/泡泡 10% (可修改)
  mainPet: 0.40,           // 主宠种类 40%
  subPet: 0.40             // 副宠种类 40%
};

// 注: mainPet和subPet的概率由 (1 - wild - special) / 2 计算得出
```

**特殊宠物说明**:
- **野生宠物**: 与主副宠等级相近的常见野生宠，通常资质较低
- **大海龟**: 特殊炼妖结果，有特定技能
- **泡泡**: 另一种稀有结果
- 野生和特殊宠物概率可由用户自定义调整

#### 1.3 技能继承规则 (核心机制)

**关键规则**: 每个技能有 **30%** 的保留概率(不可修改)

**技能池构成**:
```
技能池 = 主宠所有技能 ∪ 副宠所有技能 (去重)
```

**计算流程**:
1. 合并主副宠技能，去除重复
2. 每个技能独立进行30%概率判定
3. 通过判定的技能会保留到结果宠物
4. 检查结果宠物的必带技能(见下文)

**示例**:
```
主宠: [连击, 必杀, 强力, 偷袭]  (4个技能)
副宠: [连击, 高级反击, 防御]    (3个技能)
技能池(去重): [连击, 必杀, 强力, 偷袭, 高级反击, 防御]  (6个技能)

每个技能有30%概率保留，期望技能数 = 6 × 0.3 = 1.8个
```

### 2. 必带技能机制 (重要!)

#### 2.1 什么是必带技能

某些种族的宠物有**固定的种族必带技能**，无论炼妖结果如何，该技能**必定出现**。

**常见必带技能示例**:
- **画魂**: 高级鬼魂术、地狱烈火
- **凤凰**: 飞行、地狱烈火、高级火焰吸收、高级神佑复生
- **其他种族**: 各有特定必带技能

#### 2.2 必带技能的计算规则

```javascript
// 伪代码
function calculateFinalSkills(inheritedSkills, resultPetRace) {
  const mustHaveSkills = getMustHaveSkills(resultPetRace);
  let finalSkills = [...inheritedSkills];

  // 遍历必带技能
  for (const skill of mustHaveSkills) {
    if (!finalSkills.includes(skill)) {
      // 如果继承技能中没有必带技能，强制添加
      finalSkills.push(skill);
    }
  }

  return finalSkills;
}
```

**关键特性**:
1. 必带技能**不参与30%概率计算**，是100%出现
2. 如果技能池中已有必带技能:
   - 被30%概率选中 → 最终出现1次(不重复)
   - 未被选中 → 因为"必带"规则仍会添加
3. 必带技能可能导致技能数超过理论上限

#### 2.3 必带技能对技能数量的影响

**场景1: 技能池不包含必带技能**
```
主宠: [连击, 必杀, 强力]
副宠: [偷袭, 高级反击]
技能池: 5个，不含必带技能

假设30%概率继承了2个技能: [连击, 偷袭]
结果是画魂，必带 [高级鬼魂术, 地狱烈火]
最终技能: [连击, 偷袭, 高级鬼魂术, 地狱烈火] → 4个技能
```

**场景2: 技能池包含必带技能且被选中**
```
主宠: [连击, 高级鬼魂术, 强力]
副宠: [偷袭, 高级反击]
技能池: 5个，含 [高级鬼魂术]

假设30%概率继承了 [高级鬼魂术, 偷袭]
结果是画魂，必带 [高级鬼魂术, 地狱烈火]
最终技能: [高级鬼魂术, 偷袭, 地狱烈火] → 3个技能 (不重复添加)
```

**场景3: 技能池包含必带技能但未被选中**
```
主宠: [连击, 高级鬼魂术, 强力]
副宠: [偷袭, 高级反击]
技能池: 5个，含 [高级鬼魂术]

假设30%概率继承了 [连击, 偷袭]
结果是画魂，必带 [高级鬼魂术, 地狱烈火]
最终技能: [连击, 偷袭, 高级鬼魂术, 地狱烈火] → 4个技能 (强制添加)
```

### 3. 技能数量概率分布 (数学模型)

技能继承服从**二项分布** (Binomial Distribution):

```
P(k个技能被继承) = C(n, k) × p^k × (1-p)^(n-k)

其中:
n = 技能池大小(去重后)
k = 被继承的技能数量
p = 0.30 (30%概率)
C(n, k) = 组合数 = n! / (k! × (n-k)!)
```

**示例计算: 5个技能池**

| 继承技能数 | 概率计算 | 结果 |
|-----------|---------|------|
| 0技能 | 0.7^5 | 16.8% |
| 1技能 | C(5,1) × 0.3 × 0.7^4 | 36.0% |
| 2技能 | C(5,2) × 0.3^2 × 0.7^3 | 30.9% |
| 3技能 | C(5,3) × 0.3^3 × 0.7^2 | 13.2% |
| 4技能 | C(5,4) × 0.3^4 × 0.7 | 2.8% |
| 5技能 | 0.3^5 | 0.24% |

**期望技能数**: 5 × 0.3 = **1.5个技能**

**加上必带技能后**:
如果结果宠物有1个必带技能，期望最终技能数 = 1.5 + 1 = **2.5个技能**

### 4. 资质和成长计算

#### 4.1 资质计算公式

```javascript
// 简化模型
resultQualification = (mainPetQual + subPetQual) / 2 × randomFactor

// randomFactor 范围: 0.90 - 1.10
// 即结果资质在平均值的 90%-110% 之间波动
```

**各项资质**:
- 攻击资质: 影响物理伤害
- 防御资质: 影响物理防御
- 体力资质: 影响HP上限
- 法力资质: 影响MP上限和法术伤害
- 速度资质: 影响出手顺序

**极品标准** (以攻宠为例):
- 攻击资质 ≥1500: 优秀, ≥1600: 极品
- 体力资质 ≥4500: 优秀, ≥5000: 极品
- 速度资质 ≥1400: 优秀, ≥1500: 极品

#### 4.2 成长率计算

```javascript
resultGrowth = (mainPetGrowth + subPetGrowth) / 2 × randomFactor

// randomFactor 范围: 0.95 - 1.05
```

**成长率评级**:
- 1.00 - 1.15: 普通
- 1.16 - 1.20: 一般
- 1.21 - 1.24: 良好
- 1.25 - 1.27: 优秀
- 1.28+: 极品

**成长率的重要性**:
成长率影响宠物升级时的属性增长，极品成长的宠物在满级时属性会比普通成长高出**15-20%**。

---

## 畅玩服市场分析

### 1. 畅玩服的定义和特点

**畅玩服是什么**:
网易推出的梦幻西游付费制服务器，采用"月卡+时长点卡"模式。

**与正式服的核心区别**:

| 维度 | 畅玩服 | 正式服 |
|------|--------|--------|
| 付费模式 | 月卡制(约30-50元/月) | 免费注册，道具付费 |
| 游戏节奏 | 快速升级，降低肝度 | 传统节奏，肝度较高 |
| 经济系统 | 金币产出高，交易活跃 | 通货膨胀严重 |
| 炼妖成本 | 较低，胚子便宜 | 较高，胚子昂贵 |
| 装备获取 | 相对容易，成本低 | 需要大量投入 |
| 玩家氛围 | 休闲为主，压力小 | 竞技氛围浓，攀比严重 |

### 2. 2025年畅玩服火爆的原因

#### 数据表现
- **2025年7月**: 在线人数突破**275万**，创历史新高
- **新区开放**: 单次开放**10个新区**，开服频率加快
- **玩家增长**: 大量老玩家回归，新玩家涌入

#### 火爆原因分析

**宏观因素**:
1. **经济环境**: 玩家更倾向于"可控消费"，月卡模式相比氪金深渊更友好
2. **时间成本**: 80后、90后玩家工作繁忙，快节奏更适合碎片化时间
3. **怀旧情怀**: 梦幻西游20周年，老玩家回归潮

**游戏内因素**:
1. **公平性提升**: 减少了"人民币玩家"的碾压优势
2. **经济健康**: 新区经济稳定，没有正式服的通货膨胀问题
3. **社交体验**: 更多平民玩家，社交氛围更好
4. **活动福利**: 畅玩服专属活动，奖励丰厚

### 3. 畅玩服玩家画像

**年龄分布**:
- 主力人群: **25-35岁** (80后、90后)
- 占比: 约60-70%是有工作的成年玩家

**付费意愿**:
- 月卡接受度: **90%以上**玩家愿意付月卡费用
- 额外消费: 月均消费50-200元 (远低于正式服的500-2000元)
- 消费偏好: 月卡 > 便利道具 > 外观 > 数值提升

**游戏时长**:
- 工作日: 1-3小时/天
- 周末: 3-6小时/天
- 高峰时段: 晚上8点-12点

**游戏目的**:
- 休闲放松: 50%
- 社交互动: 30%
- 怀旧情怀: 15%
- 竞技挑战: 5%

**炼妖行为特征**:
- ✅ 更愿意尝试炼妖(成本低)
- ✅ 炼出极品后倾向于自用，而非倒卖
- ✅ 对炼妖计算器等工具需求高
- ✅ 希望了解"赌"的期望收益

### 4. 炼妖经济学

#### 4.1 成本计算方式

**注意**: 各个区物价不同，成本由用户自行填写：
- 胚子成本（用户输入）
- 单次炼妖手续费（用户输入）
- 炼妖石成本（用户输入，可选）
- 目标宠物市场价（用户输入）

#### 4.2 收益计算逻辑

**核心思路**：
1. 用户输入：胚子成本、手续费、目标技能数、目标市场价
2. 系统计算：达到目标技能数的概率
3. **重点展示**：如果打成了，能赚多少钱！
4. 期望可能是亏的，但**打成就赚钱**，激励用户尝试

**示例**：
```
胚子成本: 20万/只 × 2 = 40万
手续费: 5万
目标: 6技能宝宝
目标市场价: 500万

出6技能概率: 15%
平均需要炼: 7次
总成本: 315万

❌ 期望收益: -15万（期望亏损）
✅ 但如果打成: 赚 500万 - 45万 = 455万！
💰 运气好3次出: 赚 500万 - 135万 = 365万！

刺激文案: "虽然期望微亏，但打成一次就赚455万！你愿意试试吗？"
```

#### 4.3 打书成功率计算

**打书机制**：
- 打书成功概率 = 目标位置技能数 / 总技能数
- 例如：4技能宠物，想打掉1个垃圾技能 = 1/4 = 25%
- 打书成本：高级兽决价格（用户输入）

**打书期望计算**：
```
4技能宝宝，打掉1个垃圾技能：
- 成功率: 25%
- 平均需要打: 4本
- 高级兽决: 80万/本
- 期望成本: 320万

如果打成，宝宝从 100万 → 300万
赚: 200万 - 320万 = -120万（期望亏）

但如果1-2本就成: 赚 200万 - 160万 = 40万！
```

**激励用户逻辑**：
- 展示"如果N本内打成，赚XXX万"的表格
- 用绿色高亮盈利区间
- 用红色标注风险区间

---

## 产品设计方案

### 1. 核心产品定位

**产品名称**: 梦幻西游炼妖助手 / 西游炼妖计算器

**Slogan**:
- "科学炼妖，理性赚金"
- "算清概率再出手，不做冤大头"

**目标用户**: 畅玩服玩家，追求性价比的理性玩家

### 2. MVP功能设计 (第一阶段)

#### 2.1 炼妖概率计算器 ⭐⭐⭐⭐⭐

**功能流程**:
```
步骤1: 选择/输入主宠信息
  - 宠物种类 (从下拉列表选择)
  - 等级、资质、成长
  - 技能列表 (多选)

步骤2: 选择/输入副宠信息
  - 同上

步骤3: 设置概率参数
  - 野生概率 (默认10%，可调)
  - 特殊宠概率 (默认10%，可调)

步骤4: 输入成本信息
  - 主宠胚子价格
  - 副宠胚子价格
  - 手续费 (自动计算)

步骤5: 点击"开始计算"
```

**计算结果展示**:

**A. 结果种类概率**
```
┌─────────────────────────────┐
│ 结果宠物种类分布              │
├─────────────────────────────┤
│ 🐲 主宠(龙女)      40.0%     │
│ 🦁 副宠(狮驼)      40.0%     │
│ 🐾 野生宠物        10.0%     │
│ 🐢 特殊宠(海龟)    10.0%     │
└─────────────────────────────┘
```

**B. 技能数量概率分布** (图表 + 表格)
```
技能池: 6个技能 (去重后)
必带技能: 2个 (画魂: 高级鬼魂术、地狱烈火)

┌────────────────────────────────────┐
│ 最终技能数量概率分布 (含必带技能)    │
├────────────────────────────────────┤
│ 1技能  ████░░░░░░░░░░░░░░  16.8%  │
│ 2技能  ████████████░░░░░░  36.0%  │
│ 3技能  ██████████░░░░░░░░  30.9%  │
│ 4技能  ████░░░░░░░░░░░░░░  13.2%  │
│ 5技能  █░░░░░░░░░░░░░░░░░   2.8%  │
│ 6技能  ░░░░░░░░░░░░░░░░░░   0.24% │
└────────────────────────────────────┘

期望技能数: 2.5个
```

**C. 资质和成长预测**
```
┌─────────────────────────────────┐
│ 资质预测 (雷达图)                 │
├─────────────────────────────────┤
│ 攻击: 1350-1650 (期望: 1500)    │
│ 防御: 1200-1400 (期望: 1300)    │
│ 体力: 4200-5100 (期望: 4650)    │
│ 法力: 2100-2600 (期望: 2350)    │
│ 速度: 1300-1580 (期望: 1440)    │
└─────────────────────────────────┘

成长率: 1.18-1.26 (期望: 1.22)
```

**D. 成本收益分析** 💰
```
┌─────────────────────────────────────┐
│ 💰 成本收益分析                      │
├─────────────────────────────────────┤
│ 单次成本: 25万                       │
│ 目标: 炼出6技能极品                  │
│                                     │
│ 出6技能概率: 0.24%                  │
│ 期望炼妖次数: 417次                  │
│ 预计总成本: 1.04亿                   │
│                                     │
│ 目标宠物市场价: 8000万 (用户输入)    │
│ 期望收益: -2400万 ❌                 │
│                                     │
│ ⚠️ 建议: 目标过高，建议调整为4技能   │
└─────────────────────────────────────┘
```

**E. 多次炼妖模拟器**
```
模拟100次炼妖结果:

出现次数统计:
- 主宠种类: 39次
- 副宠种类: 42次
- 野生宠物: 11次
- 特殊宠物: 8次

技能数分布:
- 1技能: 18次
- 2技能: 35次
- 3技能: 31次
- 4技能: 13次
- 5技能: 3次
- 6技能: 0次

最佳结果: 第67次，出现5技能主宠，全资质优秀
```

#### 2.2 物价管理系统

**功能**:
- 按服务器保存物价数据
- 支持用户自定义输入
- 常见物价预设模板

**物价项目**:
```javascript
{
  server: "畅玩1区",
  prices: {
    // 胚子价格
    pets: {
      "龙女": 150000,
      "狮驼": 80000,
      // ...
    },

    // 兽决价格
    skills: {
      "高级必杀": 500000,
      "高级连击": 800000,
      // ...
    },

    // 材料价格
    materials: {
      "炼妖石": 30000,
      "兽决合成符": 50000,
    }
  }
}
```

#### 2.3 基础UI框架

**首页设计**:
```
┌───────────────────────────────────────┐
│        🎮 梦幻西游炼妖助手              │
│        科学炼妖，理性赚金               │
├───────────────────────────────────────┤
│                                       │
│   [炼妖计算器] [打书模拟器] [攻略库]   │
│                                       │
│   🔥 热门工具                          │
│   ┌─────────┐  ┌─────────┐           │
│   │ 炼妖概率 │  │ 成本分析 │           │
│   │ 计算器   │  │ 工具     │           │
│   └─────────┘  └─────────┘           │
│                                       │
│   📊 今日炼妖数据                      │
│   共计算 1,234 次                      │
│   平均期望技能数: 2.3个                │
│                                       │
│   [开始计算]                           │
└───────────────────────────────────────┘
```

**色彩方案**:
- 主色: #D4AF37 (金色)
- 辅色: #5B8C5A (青绿)
- 背景: #F5F5DC (米黄)
- 文字: #2C3E50 (深灰)

**设计风格**:
- 中国风元素: 祥云纹样、古典边框
- 避免官方美术: 使用通用的西游元素
- 移动端优先: 响应式布局

### 3. 未来功能规划 (第二、三阶段)

#### 第二阶段 (4-6周)
- 打书成功率模拟器
- 装备鉴定收益计算器
- 五开赚钱效率计算器
- 用户系统(保存历史记录)

#### 第三阶段 (7-12周)
- 藏宝图收益分析
- 社区功能(分享炼妖结果)
- 市场行情分析(爬取藏宝阁数据)
- AI炼妖助手

---

## 技术架构设计

### 1. 技术栈选择

```yaml
前端框架: Next.js 14 (App Router)
UI库: React 18 + TypeScript
样式方案: Tailwind CSS + shadcn/ui
图表库: Recharts / Chart.js
状态管理: Zustand (轻量级)
表单验证: React Hook Form + Zod

后端:
  API: Next.js API Routes
  数据库: PostgreSQL (Vercel Postgres)
  ORM: Prisma

部署:
  托管: Vercel
  CDN: CloudFlare (解决国内访问)
  域名: mhxy-helper.com

SEO:
  SSR: Next.js Server-Side Rendering
  Sitemap: 自动生成
  Meta: next/head优化
  百度统计: 集成百度统计代码

变现:
  广告: Google AdSense
```

### 2. 项目目录结构

```
mhxy_tool_web/
├── src/
│   ├── app/
│   │   ├── layout.tsx              # 根布局
│   │   ├── page.tsx                # 首页
│   │   ├── calculator/
│   │   │   └── page.tsx            # 炼妖计算器页面
│   │   ├── api/
│   │   │   ├── calculate/
│   │   │   │   └── route.ts        # 计算API
│   │   │   └── pets/
│   │   │       └── route.ts        # 宠物数据API
│   │   └── guides/
│   │       └── page.tsx            # 攻略页面
│   │
│   ├── components/
│   │   ├── calculator/
│   │   │   ├── PetSelector.tsx    # 宠物选择器
│   │   │   ├── SkillSelector.tsx  # 技能选择器
│   │   │   ├── ResultDisplay.tsx  # 结果展示
│   │   │   └── ProbabilityChart.tsx # 概率图表
│   │   ├── ui/                     # shadcn/ui组件
│   │   └── layout/
│   │       ├── Header.tsx
│   │       └── Footer.tsx
│   │
│   ├── lib/
│   │   ├── calculator/
│   │   │   ├── probability.ts     # 概率计算核心
│   │   │   ├── qualification.ts   # 资质计算
│   │   │   └── simulation.ts      # 模拟器
│   │   ├── db.ts                   # Prisma客户端
│   │   └── utils.ts                # 工具函数
│   │
│   ├── types/
│   │   ├── pet.ts                  # 宠物类型定义
│   │   ├── skill.ts                # 技能类型定义
│   │   └── calculator.ts           # 计算器类型
│   │
│   └── data/
│       ├── pets.json               # 宠物基础数据
│       ├── skills.json             # 技能数据
│       └── mustHaveSkills.json     # 必带技能映射
│
├── prisma/
│   ├── schema.prisma               # 数据库模型
│   └── seed.ts                     # 种子数据
│
├── public/
│   ├── images/
│   └── ads.txt                     # Google AdSense验证
│
├── claude.md                       # 本文档
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.js
```

### 3. 部署架构

```
┌─────────────┐
│   用户(中国)  │
└──────┬──────┘
       │
       ↓
┌─────────────────┐
│  CloudFlare CDN │  ← 解决国内访问问题
└────────┬────────┘
         │
         ↓
┌────────────────┐
│     Vercel     │  ← Next.js应用托管
├────────────────┤
│ - SSR渲染      │
│ - API Routes   │
│ - 静态资源     │
└────────┬───────┘
         │
         ↓
┌────────────────┐
│   PostgreSQL   │  ← Vercel Postgres
├────────────────┤
│ - 宠物数据     │
│ - 用户数据     │
│ - 历史记录     │
└────────────────┘

┌────────────────┐
│ Google AdSense │  ← 广告变现
└────────────────┘
```

---

## 核心算法实现

### 1. 概率计算核心算法

#### 1.1 组合数计算

```typescript
/**
 * 计算组合数 C(n, k) = n! / (k! * (n-k)!)
 */
function combination(n: number, k: number): number {
  if (k > n) return 0;
  if (k === 0 || k === n) return 1;

  let result = 1;
  for (let i = 1; i <= k; i++) {
    result = result * (n - i + 1) / i;
  }

  return result;
}
```

#### 1.2 二项分布概率计算

```typescript
/**
 * 计算二项分布概率
 * P(X = k) = C(n, k) * p^k * (1-p)^(n-k)
 *
 * @param n - 试验次数(技能池大小)
 * @param k - 成功次数(继承的技能数)
 * @param p - 成功概率(0.3)
 */
function binomialProbability(n: number, k: number, p: number): number {
  const combValue = combination(n, k);
  const probSuccess = Math.pow(p, k);
  const probFailure = Math.pow(1 - p, n - k);

  return combValue * probSuccess * probFailure;
}
```

#### 1.3 技能继承概率计算

```typescript
interface SkillProbability {
  [skillCount: number]: number;  // 技能数量 -> 概率
}

/**
 * 计算技能继承的概率分布
 *
 * @param mainPetSkills - 主宠技能列表
 * @param subPetSkills - 副宠技能列表
 * @param mustHaveSkills - 必带技能列表
 * @returns 技能数量概率分布
 */
function calculateSkillProbability(
  mainPetSkills: string[],
  subPetSkills: string[],
  mustHaveSkills: string[]
): SkillProbability {
  // 1. 合并技能池(去重)
  const skillPool = Array.from(new Set([...mainPetSkills, ...subPetSkills]));

  // 2. 移除必带技能(单独计算)
  const inheritableSkills = skillPool.filter(
    skill => !mustHaveSkills.includes(skill)
  );

  // 3. 计算每种技能数量的概率
  const n = inheritableSkills.length;
  const p = 0.30;  // 30%保留概率
  const probabilities: SkillProbability = {};

  for (let k = 0; k <= n; k++) {
    const inheritedCount = k;
    const finalCount = inheritedCount + mustHaveSkills.length;
    const probability = binomialProbability(n, k, p);

    probabilities[finalCount] = probability;
  }

  return probabilities;
}
```

#### 1.4 结果种类概率计算

```typescript
interface ResultProbability {
  mainPet: number;
  subPet: number;
  wild: number;
  special: number;
}

/**
 * 计算结果宠物种类的概率分布
 *
 * @param wildProb - 野生概率(用户可调)
 * @param specialProb - 特殊宠概率(用户可调)
 * @returns 各种结果的概率
 */
function calculateResultTypeProbability(
  wildProb: number = 0.10,
  specialProb: number = 0.10
): ResultProbability {
  const remaining = 1 - wildProb - specialProb;

  return {
    mainPet: remaining / 2,
    subPet: remaining / 2,
    wild: wildProb,
    special: specialProb
  };
}
```

### 2. 资质和成长计算

```typescript
interface Qualification {
  attack: number;
  defense: number;
  hp: number;
  mana: number;
  speed: number;
}

interface QualificationRange {
  min: number;
  max: number;
  expected: number;
}

/**
 * 计算资质的预测范围
 *
 * @param mainQual - 主宠资质
 * @param subQual - 副宠资质
 * @returns 预测的资质范围
 */
function calculateQualificationRange(
  mainQual: Qualification,
  subQual: Qualification
): Record<keyof Qualification, QualificationRange> {
  const result = {} as Record<keyof Qualification, QualificationRange>;

  const keys: (keyof Qualification)[] = ['attack', 'defense', 'hp', 'mana', 'speed'];

  for (const key of keys) {
    const avg = (mainQual[key] + subQual[key]) / 2;

    result[key] = {
      min: Math.floor(avg * 0.90),
      max: Math.ceil(avg * 1.10),
      expected: Math.round(avg)
    };
  }

  return result;
}

/**
 * 计算成长率的预测范围
 */
function calculateGrowthRange(
  mainGrowth: number,
  subGrowth: number
): QualificationRange {
  const avg = (mainGrowth + subGrowth) / 2;

  return {
    min: parseFloat((avg * 0.95).toFixed(2)),
    max: parseFloat((avg * 1.05).toFixed(2)),
    expected: parseFloat(avg.toFixed(2))
  };
}
```

### 3. 成本收益分析

```typescript
interface CostBenefitAnalysis {
  singleCost: number;              // 单次成本
  targetSkillCount: number;        // 目标技能数
  targetProbability: number;       // 达到目标的概率
  expectedAttempts: number;        // 期望尝试次数
  totalCost: number;               // 预计总成本
  marketPrice: number;             // 市场价格
  expectedProfit: number;          // 期望收益
  profitable: boolean;             // 是否有利可图
  recommendation: string;          // 建议
}

/**
 * 计算成本收益分析
 *
 * @param mainPetPrice - 主宠价格
 * @param subPetPrice - 副宠价格
 * @param handlingFee - 手续费
 * @param targetSkillCount - 目标技能数
 * @param skillProbabilities - 技能数量概率分布
 * @param marketPrice - 目标宠物市场价格
 */
function calculateCostBenefit(
  mainPetPrice: number,
  subPetPrice: number,
  handlingFee: number,
  targetSkillCount: number,
  skillProbabilities: SkillProbability,
  marketPrice: number
): CostBenefitAnalysis {
  const singleCost = mainPetPrice + subPetPrice + handlingFee;
  const targetProbability = skillProbabilities[targetSkillCount] || 0;

  // 期望尝试次数 = 1 / 概率 (几何分布)
  const expectedAttempts = targetProbability > 0
    ? Math.ceil(1 / targetProbability)
    : Infinity;

  const totalCost = singleCost * expectedAttempts;
  const expectedProfit = marketPrice - totalCost;
  const profitable = expectedProfit > 0;

  let recommendation = '';
  if (targetProbability < 0.01) {
    recommendation = '⚠️ 目标过高，成功概率极低，建议降低目标技能数';
  } else if (!profitable) {
    recommendation = '❌ 预期亏损，不建议炼妖。考虑提高胚子质量或降低目标';
  } else if (expectedProfit > totalCost * 0.5) {
    recommendation = '✅ 高收益项目！值得尝试';
  } else {
    recommendation = '💰 有利可图，但收益率一般，谨慎决策';
  }

  return {
    singleCost,
    targetSkillCount,
    targetProbability,
    expectedAttempts: expectedAttempts === Infinity ? 0 : expectedAttempts,
    totalCost: totalCost === Infinity ? 0 : totalCost,
    marketPrice,
    expectedProfit: expectedProfit === Infinity ? -Infinity : expectedProfit,
    profitable,
    recommendation
  };
}
```

### 4. 多次炼妖模拟器

```typescript
interface SimulationResult {
  attemptNumber: number;           // 第几次
  resultType: 'mainPet' | 'subPet' | 'wild' | 'special';
  skillCount: number;
  qualifications: Qualification;
  growth: number;
  isTarget: boolean;               // 是否达到目标
}

interface SimulationSummary {
  totalAttempts: number;
  resultTypeCounts: Record<string, number>;
  skillCountDistribution: Record<number, number>;
  bestResult: SimulationResult;
  targetAchieved: boolean;
  achievedAtAttempt: number;
}

/**
 * 模拟多次炼妖
 *
 * @param attempts - 模拟次数
 * @param mainPet - 主宠信息
 * @param subPet - 副宠信息
 * @param targetSkillCount - 目标技能数
 * @returns 模拟结果汇总
 */
function simulateRefinement(
  attempts: number,
  mainPet: Pet,
  subPet: Pet,
  targetSkillCount: number
): SimulationSummary {
  const results: SimulationResult[] = [];
  const resultTypeCounts: Record<string, number> = {
    mainPet: 0,
    subPet: 0,
    wild: 0,
    special: 0
  };
  const skillCountDistribution: Record<number, number> = {};

  let bestResult: SimulationResult | null = null;
  let targetAchieved = false;
  let achievedAtAttempt = 0;

  for (let i = 1; i <= attempts; i++) {
    // 1. 随机确定结果种类
    const resultType = randomResultType();
    resultTypeCounts[resultType]++;

    // 2. 随机继承技能
    const skillCount = randomSkillCount(mainPet.skills, subPet.skills);
    skillCountDistribution[skillCount] = (skillCountDistribution[skillCount] || 0) + 1;

    // 3. 随机资质
    const qualifications = randomQualifications(mainPet.qualifications, subPet.qualifications);

    // 4. 随机成长
    const growth = randomGrowth(mainPet.growth, subPet.growth);

    const result: SimulationResult = {
      attemptNumber: i,
      resultType,
      skillCount,
      qualifications,
      growth,
      isTarget: skillCount >= targetSkillCount
    };

    results.push(result);

    // 检查是否达到目标
    if (result.isTarget && !targetAchieved) {
      targetAchieved = true;
      achievedAtAttempt = i;
    }

    // 更新最佳结果
    if (!bestResult || skillCount > bestResult.skillCount) {
      bestResult = result;
    }
  }

  return {
    totalAttempts: attempts,
    resultTypeCounts,
    skillCountDistribution,
    bestResult: bestResult!,
    targetAchieved,
    achievedAtAttempt
  };
}

// 辅助函数：随机确定结果种类
function randomResultType(): 'mainPet' | 'subPet' | 'wild' | 'special' {
  const rand = Math.random();
  if (rand < 0.40) return 'mainPet';
  if (rand < 0.80) return 'subPet';
  if (rand < 0.90) return 'wild';
  return 'special';
}

// 辅助函数：随机继承技能数量
function randomSkillCount(mainSkills: string[], subSkills: string[]): number {
  const skillPool = Array.from(new Set([...mainSkills, ...subSkills]));
  let count = 0;

  for (const skill of skillPool) {
    if (Math.random() < 0.30) {
      count++;
    }
  }

  // 加上必带技能(简化处理，实际需要根据结果种类判断)
  count += 1;

  return count;
}

// 辅助函数：随机资质
function randomQualifications(
  mainQual: Qualification,
  subQual: Qualification
): Qualification {
  const result = {} as Qualification;
  const keys: (keyof Qualification)[] = ['attack', 'defense', 'hp', 'mana', 'speed'];

  for (const key of keys) {
    const avg = (mainQual[key] + subQual[key]) / 2;
    const factor = 0.90 + Math.random() * 0.20; // 0.90-1.10
    result[key] = Math.round(avg * factor);
  }

  return result;
}

// 辅助函数：随机成长
function randomGrowth(mainGrowth: number, subGrowth: number): number {
  const avg = (mainGrowth + subGrowth) / 2;
  const factor = 0.95 + Math.random() * 0.10; // 0.95-1.05
  return parseFloat((avg * factor).toFixed(2));
}
```

---

## 数据库设计

### 1. Prisma Schema

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 宠物基础数据表
model Pet {
  id                String   @id @default(cuid())
  name              String   @unique
  level             Int
  race              String   // 种族(龙族、凤族等)

  // 资质范围
  attackMin         Int
  attackMax         Int
  defenseMin        Int
  defenseMax        Int
  hpMin             Int
  hpMax             Int
  manaMin           Int
  manaMax           Int
  speedMin          Int
  speedMax          Int

  // 成长范围
  growthMin         Float
  growthMax         Float

  // 技能(JSON数组)
  skills            Json     // string[]

  // 必带技能(JSON数组)
  mustHaveSkills    Json     // string[]

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([name])
  @@index([race])
}

// 技能数据表
model Skill {
  id                String   @id @default(cuid())
  name              String   @unique
  type              String   // 攻击、防御、辅助、特殊
  description       String
  isAdvanced        Boolean  @default(false) // 是否高级技能

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([name])
  @@index([type])
}

// 服务器物价数据表
model ServerPrice {
  id                String   @id @default(cuid())
  serverName        String

  // 物价数据(JSON)
  petPrices         Json     // { petName: price }
  skillPrices       Json     // { skillName: price }
  materialPrices    Json     // { materialName: price }

  updatedBy         String?  // 更新者(用户ID)

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@unique([serverName])
  @@index([serverName])
}

// 炼妖历史记录表 (Phase 2+)
model RefinementRecord {
  id                String   @id @default(cuid())
  userId            String?  // 用户ID(可选，未登录用户为null)

  // 输入数据
  mainPetName       String
  subPetName        String
  mainPetSkills     Json     // string[]
  subPetSkills      Json     // string[]

  // 计算结果
  skillProbabilities Json    // SkillProbability
  expectedSkillCount Float

  // 成本收益
  totalCost         Int
  expectedProfit    Int

  createdAt         DateTime @default(now())

  @@index([userId])
  @@index([createdAt])
}

// 用户表 (Phase 2+)
model User {
  id                String   @id @default(cuid())
  username          String   @unique
  email             String?  @unique

  favoriteServer    String?  // 常用服务器

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([username])
}
```

### 2. 种子数据示例

```typescript
// prisma/seed.ts

import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

async function main() {
  // 宠物数据
  const pets = [
    {
      name: '龙女',
      level: 95,
      race: '龙族',
      attackMin: 1200,
      attackMax: 1600,
      defenseMin: 1100,
      defenseMax: 1500,
      hpMin: 4000,
      hpMax: 5500,
      manaMin: 2000,
      manaMax: 2800,
      speedMin: 1200,
      speedMax: 1500,
      growthMin: 1.15,
      growthMax: 1.28,
      skills: ['连击', '必杀', '强力', '高级龙之力'],
      mustHaveSkills: ['高级龙之力']
    },
    {
      name: '凤凰',
      level: 95,
      race: '凤族',
      attackMin: 1000,
      attackMax: 1400,
      defenseMin: 1200,
      defenseMax: 1600,
      hpMin: 4500,
      hpMax: 6000,
      manaMin: 2500,
      manaMax: 3500,
      speedMin: 1100,
      speedMax: 1400,
      growthMin: 1.18,
      growthMax: 1.30,
      skills: ['飞行', '地狱烈火', '高级火焰吸收', '高级神佑复生'],
      mustHaveSkills: ['飞行', '地狱烈火']
    },
    // ... 更多宠物数据
  ];

  for (const pet of pets) {
    await prisma.pet.upsert({
      where: { name: pet.name },
      update: {},
      create: pet
    });
  }

  // 技能数据
  const skills = [
    {
      name: '连击',
      type: '攻击',
      description: '攻击时有几率连续攻击2-3次',
      isAdvanced: false
    },
    {
      name: '高级连击',
      type: '攻击',
      description: '攻击时有较高几率连续攻击2-3次',
      isAdvanced: true
    },
    // ... 更多技能数据
  ];

  for (const skill of skills) {
    await prisma.skill.upsert({
      where: { name: skill.name },
      update: {},
      create: skill
    });
  }

  // 默认物价数据
  await prisma.serverPrice.upsert({
    where: { serverName: '畅玩1区' },
    update: {},
    create: {
      serverName: '畅玩1区',
      petPrices: {
        '龙女': 150000,
        '凤凰': 180000,
        '狮驼': 80000
      },
      skillPrices: {
        '高级必杀': 500000,
        '高级连击': 800000,
        '高级神佑复生': 1200000
      },
      materialPrices: {
        '炼妖石': 30000,
        '兽决合成符': 50000
      }
    }
  });

  console.log('✅ 数据库种子数据已创建');
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

---

## SEO与变现策略

### 1. SEO优化方案

#### 1.0 域名配置最佳实践 ⭐⭐⭐⭐⭐

**当前域名配置**:
- 主域名: `www.mhxy-helper.com`
- 次域名: `mhxy-helper.com` (307 重定向到主域名)

**SEO 最佳实践建议**:

##### 推荐方案：使用 www 作为主域名 ✅

**优势**:
1. **规范化 (Canonicalization)**:
   - 避免重复内容问题，搜索引擎明确知道哪个是主域名
   - 所有外链、引用、收录都集中到 `www.mhxy-helper.com`

2. **Cookie 管理优势**:
   - `www` 子域名的 Cookie 不会发送到其他子域名（如 `api.mhxy-helper.com`）
   - 减少不必要的 Cookie 传输，提升性能

3. **CDN 和子域名扩展性**:
   - 未来如果需要 `api.mhxy-helper.com`, `cdn.mhxy-helper.com` 等子域名更灵活
   - 不会与主站的 Cookie 冲突

4. **符合传统习惯**:
   - 用户习惯输入 `www`，看起来更专业
   - 大型网站如 `www.baidu.com`, `www.taobao.com` 都使用 www

**配置要点**:

1. **301/307 重定向设置** ✅ (当前已配置)
   ```
   mhxy-helper.com → 307 重定向 → www.mhxy-helper.com
   ```
   - 使用 **301 永久重定向** 更好（告诉搜索引擎永久迁移）
   - 307 临时重定向也可以，但 SEO 效果略差

2. **Canonical 标签配置** ⭐ (必须配置)
   ```html
   <link rel="canonical" href="https://www.mhxy-helper.com/当前页面路径" />
   ```
   - 所有页面都应该指向 `www` 版本
   - 即使用户访问 `mhxy-helper.com`，canonical 也指向 `www` 版本

3. **Sitemap 配置**
   ```xml
   <?xml version="1.0" encoding="UTF-8"?>
   <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
     <url>
       <loc>https://www.mhxy-helper.com/</loc>
       <lastmod>2025-01-05</lastmod>
       <priority>1.0</priority>
     </url>
     <url>
       <loc>https://www.mhxy-helper.com/calculator</loc>
       ...
     </url>
   </urlset>
   ```
   - 所有 URL 都使用 `www` 版本

4. **Google Search Console**
   - 提交 `https://www.mhxy-helper.com/` 作为主域名
   - 可以同时添加 `https://mhxy-helper.com/`，但设置 `www` 为首选域名

5. **内部链接统一**
   - 所有内部链接使用相对路径 `/calculator` 或绝对路径 `https://www.mhxy-helper.com/calculator`
   - 不要混用 `mhxy-helper.com` 和 `www.mhxy-helper.com`

6. **Open Graph 和 Twitter Card**
   ```typescript
   openGraph: {
     url: 'https://www.mhxy-helper.com/calculator',  // 使用 www
     // ...
   }
   ```

**检查清单**:
- [x] 设置 307/301 重定向: `mhxy-helper.com` → `www.mhxy-helper.com`
- [ ] 所有页面添加 canonical 标签指向 `www` 版本
- [ ] 更新 sitemap.xml 使用 `www` URL
- [ ] 更新所有 Open Graph 和 meta 标签使用 `www` URL
- [ ] Google Search Console 提交 `www` 版本
- [ ] 百度站长平台提交 `www` 版本
- [ ] 检查内部链接都使用 `www` 或相对路径

**注意事项**:
- ⚠️ 建议将 307 改为 **301 永久重定向**，这样搜索引擎会更快地识别主域名
- ⚠️ 一旦选择了 `www` 作为主域名，就不要再改变，保持一致性
- ⚠️ 所有外部推广、社交媒体分享都使用 `https://www.mhxy-helper.com`

---

#### 1.1 关键词策略

**核心关键词**:
- 梦幻西游炼妖计算器
- 梦幻西游炼妖概率
- 梦幻西游炼妖模拟器
- 梦幻西游畅玩服攻略

**长尾关键词**:
- 梦幻西游怎么炼妖出多技能
- 梦幻西游炼妖成本计算
- 梦幻西游炼妖赚钱攻略
- 梦幻西游必带技能规则

#### 1.2 页面SEO配置

```typescript
// app/calculator/page.tsx

import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '梦幻西游炼妖计算器 - 精确概率计算 | 炼妖助手',
  description: '专业的梦幻西游炼妖概率计算器，支持技能继承、必带技能、成本分析。畅玩服玩家必备工具，科学炼妖不做冤大头。',
  keywords: '梦幻西游,炼妖计算器,炼妖概率,畅玩服,多技能,成本分析',
  openGraph: {
    title: '梦幻西游炼妖计算器',
    description: '精确计算炼妖概率，助你科学炼出极品宠物',
    type: 'website',
    url: 'https://mhxy-helper.com/calculator',
    images: ['/og-image.jpg']
  }
};

export default function CalculatorPage() {
  return (
    <>
      <h1>梦幻西游炼妖概率计算器</h1>
      {/* ... */}
    </>
  );
}
```

#### 1.3 结构化数据

```typescript
// 添加JSON-LD结构化数据
const structuredData = {
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "梦幻西游炼妖计算器",
  "url": "https://mhxy-helper.com",
  "description": "专业的梦幻西游炼妖概率计算工具",
  "applicationCategory": "GameApplication",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "CNY"
  }
};
```

#### 1.4 Sitemap生成

```typescript
// app/sitemap.ts

import { MetadataRoute } from 'next';

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: 'https://mhxy-helper.com',
      lastModified: new Date(),
      changeFrequency: 'daily',
      priority: 1
    },
    {
      url: 'https://mhxy-helper.com/calculator',
      lastModified: new Date(),
      changeFrequency: 'daily',
      priority: 0.9
    },
    {
      url: 'https://mhxy-helper.com/guides',
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 0.8
    }
  ];
}
```

#### 1.5 百度统计集成

```typescript
// app/layout.tsx

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh-CN">
      <head>
        {/* 百度统计 */}
        <script
          dangerouslySetInnerHTML={{
            __html: `
              var _hmt = _hmt || [];
              (function() {
                var hm = document.createElement("script");
                hm.src = "https://hm.baidu.com/hm.js?YOUR_BAIDU_ID";
                var s = document.getElementsByTagName("script")[0];
                s.parentNode.insertBefore(hm, s);
              })();
            `
          }}
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
```

### 2. Google AdSense变现策略

#### 2.1 广告位布局

```
┌─────────────────────────────────────┐
│           Header                    │
├──────────┬──────────────────────────┤
│          │                          │
│ Sidebar  │   Main Content           │
│ Ad Unit  │   (计算器)                │
│ (300x600)│                          │
│          │                          │
│          ├──────────────────────────┤
│          │ Bottom Ad (728x90)       │
└──────────┴──────────────────────────┘

移动端:
┌─────────────────┐
│   Header        │
├─────────────────┤
│   Main Content  │
│   (计算器)       │
├─────────────────┤
│ Ad Unit (320x50)│
├─────────────────┤
│   Results       │
├─────────────────┤
│ Ad Unit (300x250)│
└─────────────────┘
```

#### 2.2 AdSense代码集成

```typescript
// components/AdUnit.tsx

'use client';

import { useEffect } from 'react';

interface AdUnitProps {
  slot: string;
  format?: string;
  responsive?: boolean;
}

export function AdUnit({ slot, format = 'auto', responsive = true }: AdUnitProps) {
  useEffect(() => {
    try {
      ((window as any).adsbygoogle = (window as any).adsbygoogle || []).push({});
    } catch (err) {
      console.error('AdSense error:', err);
    }
  }, []);

  return (
    <ins
      className="adsbygoogle"
      style={{ display: 'block' }}
      data-ad-client="ca-pub-YOUR_PUBLISHER_ID"
      data-ad-slot={slot}
      data-ad-format={format}
      data-full-width-responsive={responsive.toString()}
    />
  );
}
```

#### 2.3 收益优化策略

**广告位置优化**:
- ✅ 计算结果下方 (用户完成计算后注意力最集中)
- ✅ 侧边栏 (桌面端长时间展示)
- ❌ 避免遮挡核心功能 (影响用户体验)

**广告类型选择**:
- 自动广告 (Auto Ads): 让Google自动优化位置
- 展示广告: 300x250, 728x90 (高转化率)
- 信息流广告: 融入内容，不突兀

**收益预估** (中国流量):
```
日UV 1000:  收入约 $1-3/天   = $30-90/月
日UV 5000:  收入约 $5-15/天  = $150-450/月
日UV 10000: 收入约 $10-30/天 = $300-900/月
```

#### 2.4 ads.txt配置

```
# public/ads.txt
google.com, pub-YOUR_PUBLISHER_ID, DIRECT, f08c47fec0942fa0
```

---

## 侵权规避策略

### 1. 商标和名称规避

#### 禁止使用
❌ "梦幻西游" 官方Logo
❌ 网易公司Logo
❌ 官方吉祥物形象
❌ 声称"官方工具"、"官方授权"

#### 安全使用
✅ 文字描述: "梦幻西游炼妖计算器" (描述性使用)
✅ 在About页面声明: "本站为非官方玩家工具，与网易公司无关"
✅ 在Footer添加: "游戏版权归网易公司所有"

### 2. 美术资源规避

#### 禁止使用
❌ 游戏截图 (除非用于评论/教学，且标注来源)
❌ 角色立绘
❌ 场景图片
❌ UI素材

#### 替代方案
✅ 使用通用的西游元素:
- 祥云纹样 (公共领域)
- 古典边框 (传统纹样)
- 水墨风格插画 (原创设计)

✅ 购买商用素材:
- 站酷、千图网等平台的中国风素材
- Unsplash、Pexels的免费图片

✅ 原创设计:
- 简约的图标设计
- 抽象的几何图形
- 数据可视化图表

### 3. 宠物/技能名称处理

#### 方案A: 直接使用(风险较低)
宠物和技能名称通常不受版权保护(仅商标保护)，可以直接使用:
- "龙女"、"凤凰" (通用名词)
- "连击"、"必杀" (游戏术语)

**法律依据**: 游戏规则和数据不受版权保护 (美国: Baker v. Selden案例)

#### 方案B: 描述性名称(最安全)
如果担心风险，使用描述性名称:
- "龙女" → "龙族召唤兽A"
- "高级必杀" → "攻击技能-必杀(高级)"

### 4. 数据来源声明

在网站底部或About页面添加:

```markdown
## 数据来源声明

本站所有游戏数据均来源于:
1. 玩家社区公开分享的信息
2. 游戏内公开展示的数据
3. 第三方数据库(如17173、藏宝阁)

本站不对数据的准确性做绝对保证，仅供参考。

## 版权声明

《梦幻西游》游戏版权归网易公司所有。
本站为非官方玩家工具，与网易公司无任何关联。
如有侵权，请联系我们立即删除。
```

### 5. 参考案例(安全运营多年)

以下第三方工具运营多年未被起诉:
- 17173梦幻西游工具箱
- 叶子猪梦幻西游专区
- 追梦工具箱 (iOS App)

**成功经验**:
1. 不使用官方Logo和美术素材
2. 明确标注"非官方"
3. 不进行虚假宣传
4. 提供实用价值，不损害游戏利益

---

## 开发路线图

### Phase 1: MVP开发 (2-3周)

#### Week 1
- [x] 项目初始化 (Next.js + TypeScript)
- [x] 数据库设计 (Prisma Schema)
- [x] 核心算法实现 (概率计算)
- [ ] 基础UI组件 (Layout, Header, Footer)

#### Week 2
- [ ] 宠物数据库 (至少20种宠物数据)
- [ ] 技能数据库 (至少50种技能)
- [ ] 炼妖计算器页面
- [ ] 宠物选择器组件
- [ ] 技能选择器组件

#### Week 3
- [ ] 结果展示组件 (概率分布图表)
- [ ] 成本收益分析模块
- [ ] 物价输入系统
- [ ] 响应式设计优化
- [ ] SEO基础配置

### Phase 2: 功能完善 (4-6周)

#### Week 4-5
- [ ] 多次炼妖模拟器
- [ ] 历史记录功能
- [ ] 数据导出功能 (CSV/JSON)
- [ ] 性能优化

#### Week 6
- [ ] Google AdSense集成
- [ ] 百度统计集成
- [ ] CloudFlare CDN配置
- [ ] 域名解析和部署

### Phase 3: 内容和SEO (7-10周)

#### Week 7-9
- [ ] 攻略文章库 (10-20篇)
- [ ] 关键词优化
- [ ] 外链建设
- [ ] 百度站长平台提交

#### Week 10
- [ ] 用户反馈收集
- [ ] Bug修复
- [ ] 性能监控
- [ ] A/B测试 (广告位优化)

### Phase 4: 高级功能 (11-16周)

- [ ] 打书成功率模拟器
- [ ] 装备鉴定计算器
- [ ] 用户系统 (注册/登录)
- [ ] 社区功能 (分享炼妖结果)
- [ ] 市场行情分析 (爬虫)
- [ ] AI炼妖助手

---

## 附录

### A. 技术债务和注意事项

1. **数据准确性**: 必带技能规则需要持续验证和更新
2. **性能优化**: 模拟10000次可能耗时较长，需要Web Worker
3. **缓存策略**: 计算结果可以缓存，减少重复计算
4. **移动端体验**: 表单输入较多，需要优化交互流程

### B. 待验证的问题

1. **概率参数**: 野生10%、特殊10%需要大数据验证
2. **必带技能列表**: 需要建立完整的宠物-必带技能映射表
3. **资质随机系数**: 0.9-1.1的范围需要验证
4. **成长随机系数**: 0.95-1.05的范围需要验证

### C. 未来扩展方向

1. **移动App**: 开发iOS/Android原生App
2. **微信小程序**: 覆盖微信生态用户
3. **数据API**: 提供数据API供其他开发者使用
4. **国际化**: 支持繁体中文，覆盖台湾、香港玩家
5. **会员系统**: 高级功能付费订阅

### D. 运营指标

**关键指标 (KPI)**:
- 日活用户 (DAU)
- 计算次数/用户
- 页面停留时长
- 广告点击率 (CTR)
- 广告收入 (Revenue)

**目标**:
- 3个月: DAU 500, 月收入 $100
- 6个月: DAU 2000, 月收入 $500
- 12个月: DAU 5000, 月收入 $1500

---

## 结语

本文档详细阐述了梦幻西游炼妖助手项目的调研结果、产品设计、技术方案和实施路线。

**核心竞争力**:
1. **精确的概率计算** - 基于数学模型，而非经验估算
2. **完整的必带技能支持** - 其他工具缺少的关键功能
3. **成本收益分析** - 帮助玩家做出理性决策
4. **现代化用户体验** - 响应式设计，移动端友好

**市场机会**:
- 畅玩服火爆，在线人数创新高
- 现有工具功能简陋，用户需求未被满足
- 中国游戏玩家基数大，流量变现潜力高

**风险控制**:
- 技术风险: 使用成熟技术栈，降低开发风险
- 法律风险: 不使用官方资源，明确标注非官方
- 商业风险: 依赖AdSense单一收入，未来需多元化

**下一步行动**:
1. ✅ 完成本调研文档
2. ⏭️ 初始化Next.js项目
3. ⏭️ 实现核心算法
4. ⏭️ 构建MVP版本
5. ⏭️ 部署上线并推广

让我们开始构建这个有趣且有价值的产品吧！🚀

---

**文档版本**: v1.0
**最后更新**: 2025-10-30
**作者**: Claude & 用户
**项目状态**: 规划阶段 → 即将进入开发阶段
